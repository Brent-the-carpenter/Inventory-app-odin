extends layout

block content
  h1= title
  if (error != undefined)
    h1= error.message
    h2= error.status
    pre #{error.stack}
  else
    h1= message 
    h2 status: #{status}
    p= description

